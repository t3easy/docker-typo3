version: '3.5'
services:
  web:
    build:
      context: .
      dockerfile: .docker/Dockerfile.prod
      args:
        TARGET_SERVICE: web

  typo3:
    build:
      context: .
      dockerfile: .docker/Dockerfile.prod
      args:
        TARGET_SERVICE: php